---
phase: 02-order-cart-address
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/common/interceptors/transform-response.interceptor.ts
autonomous: true
requirements: [CART-01]
user_setup: []

must_haves:
  truths:
    - 'Project depends on nanoid@3 as a production dependency, importable as named export `nanoid`'
    - 'Any HTTP response with statusCode 204 leaves the controller/interceptor pipeline with an empty body — no JSON wrapper'
    - 'Existing 200/201 wrapped responses continue to be wrapped by TransformResponseInterceptor'
  artifacts:
    - path: 'package.json'
      provides: 'nanoid@^3 in dependencies'
      contains: '"nanoid"'
    - path: 'src/common/interceptors/transform-response.interceptor.ts'
      provides: '204 bypass branch inside map() callback'
      contains: 'statusCode === 204'
  key_links:
    - from: 'src/common/interceptors/transform-response.interceptor.ts'
      to: 'all controllers returning 204'
      via: 'early-return in map() when response.statusCode === 204'
      pattern: "if \\(statusCode === 204\\)"
---

<objective>
Install nanoid@3 (CJS-compatible) and patch TransformResponseInterceptor so that any 204 response bypasses the JSON envelope. This is the shared infrastructure that Plan 03 (order number) and Plan 04 (cart 204) build on.

Purpose: nanoid is required by ORD-02. The interceptor bypass (D-01, D-02) is the single fix point covering DELETE /cart, DELETE /cart/items/:id, the new PATCH /cart/items/:id (qty=0) case, and any future 204 routes.
Output: Working dependency in package.json + interceptor with 204 short-circuit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-order-cart-address/02-SPEC.md
@.planning/phases/02-order-cart-address/02-CONTEXT.md
@.planning/phases/02-order-cart-address/02-RESEARCH.md
@.planning/phases/02-order-cart-address/02-PATTERNS.md
@src/common/interceptors/transform-response.interceptor.ts
@package.json
</context>

<interfaces>
<!-- Current interceptor map() callback shape (lines 44-76 of transform-response.interceptor.ts). The 204 branch MUST be inserted as the FIRST check inside the map callback, before the existing `data === null` branch. -->

Existing map callback flow:

1. read `statusCode = response.statusCode`
2. (NEW — insert here) if statusCode === 204 → return undefined as unknown as Response<T>
3. if data is null/undefined → return { statusCode, success, message }
4. if data already has success+message keys → spread
5. else wrap as { statusCode, success, message, data }

nanoid v3 named import:

```
import { nanoid } from 'nanoid';
const id = nanoid(6); // URL-safe 6-char string
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install nanoid@3 as production dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verify no existing `"type": "module"` field — confirms CommonJS default, locks nanoid@3 per D-05)
  </read_first>
  <action>
    Run `npm install nanoid@3` from project root. Per D-05/D-06, this MUST be the v3 line (not v4+) because the project is CommonJS — nanoid@4 is pure ESM and would throw ERR_REQUIRE_ESM at runtime.

    After install, verify package.json `dependencies.nanoid` is present and resolves to a `^3.x` range (typically `^3.3.12`). Do NOT add to devDependencies.

    Do not modify any source file in this task — installation only.

  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const v=p.dependencies && p.dependencies.nanoid; if(!v || !v.includes('3')) { console.error('nanoid@3 not installed, got:', v); process.exit(1); } const n=require('nanoid'); if(typeof n.nanoid!=='function'){console.error('named export nanoid missing'); process.exit(1);} console.log('OK', v, n.nanoid(6));"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies object contains a `"nanoid"` key with a version string starting with `"^3"` or `"3."`
    - `require('nanoid').nanoid` is a function (named export resolvable in CommonJS)
    - Calling `require('nanoid').nanoid(6)` returns a 6-character string
    - package.json has NO `"type": "module"` field added
    - nanoid does NOT appear in devDependencies
  </acceptance_criteria>
  <done>nanoid@3 is installed and importable as `import { nanoid } from 'nanoid'`; verification command above exits 0 and prints a 6-char id.</done>
</task>

<task type="auto">
  <name>Task 2: Add 204 bypass branch to TransformResponseInterceptor</name>
  <files>src/common/interceptors/transform-response.interceptor.ts</files>
  <read_first>
    - src/common/interceptors/transform-response.interceptor.ts (current file — lines 43-76 contain the map() callback to modify)
    - .planning/phases/02-order-cart-address/02-PATTERNS.md §"transform-response.interceptor.ts — 204 Bypass" (lines 489-553) for the exact insertion block
  </read_first>
  <action>
    Inside the existing `next.handle().pipe(map((data: unknown) => { ... }))` block, immediately after the line `const statusCode = response.statusCode;`, insert a new branch that returns `undefined as unknown as Response<T>` when `statusCode === 204`. This branch MUST be the FIRST check — placed before the existing `if (data === null || data === undefined)` block. Per D-01/D-02 this is the single fix point that covers DELETE /cart, DELETE /cart/items/:id, the new PATCH /cart/items/:id (qty=0) case, and any future 204 routes.

    Do NOT switch the pipeline from `map` to `switchMap`. Do NOT change the interceptor signature, constructor, or `getDefaultMessage`. No new imports are needed — `map` from `rxjs/operators` is already imported on line 4. Type cast `undefined as unknown as Response<T>` keeps the return type compatible with the declared `Observable<Response<T>>`.

    Leave the existing null/undefined branch (currently lines 47-54) UNCHANGED — it must keep wrapping non-204 null responses with the `{ statusCode, success, message }` shape so that 200+null endpoints still get the envelope.

  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/common/interceptors/transform-response.interceptor.ts contains the literal substring `statusCode === 204` exactly once
    - The 204 branch appears textually BEFORE the substring `data === null || data === undefined` in the file
    - The 204 branch sits INSIDE the `map((data: unknown) => { ... })` callback (not at module scope, not in a separate operator)
    - `npm run build` exits 0 with no TypeScript errors
    - File still exports `TransformResponseInterceptor` as the default class export pattern (no rename)
    - No new top-level imports added beyond what already exists at lines 1-6
  </acceptance_criteria>
  <done>Interceptor returns undefined (empty body) for any 204 response; existing 200/201/non-204-null behaviors are unchanged; build is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                     |
| --------------------------------- | --------------------------------------------------------------- |
| npm registry → local node_modules | Untrusted package code installed; legitimacy gate required      |
| HTTP client → NestJS interceptor  | Response shaping crosses here; status-code drives body presence |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | nanoid package install                  | mitigate    | Pin to `nanoid@^3` from npm registry; RESEARCH.md §Package Legitimacy Audit confirms author `ai` (Andrey Sitnik), no postinstall script, >200M weekly downloads — Approved disposition. Verify `npm view nanoid@3.3.12` repository field matches `github.com/ai/nanoid` at install time. |
| T-02-02   | Information Disclosure | TransformResponseInterceptor 204 branch | mitigate    | 204 branch returns `undefined` — NestJS will not write any body, preventing leakage of internal `{ statusCode, success, message }` envelope on intentionally-empty responses (DELETE, cart removal).                                                                                     |
| T-02-03   | Denial of Service      | nanoid import on hot path               | accept      | Pure in-process call, no I/O, ~µs cost; not exploitable.                                                                                                                                                                                                                                 |
| T-02-SC   | Tampering              | nanoid supply chain                     | mitigate    | Audit table in RESEARCH.md §Package Legitimacy Audit marks nanoid as `[OK]` (Approved). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking-human checkpoint required.                                                                                                                |

</threat_model>

<verification>
- `npm run build` exits 0
- `node -e "console.log(require('nanoid').nanoid(6))"` prints a 6-char string
- Grep confirms exactly one occurrence of `statusCode === 204` in the interceptor file
</verification>

<success_criteria>

- nanoid@^3 in package.json dependencies and resolvable via CommonJS `require`
- TransformResponseInterceptor short-circuits to empty body when `response.statusCode === 204`
- No regressions for 200/201/non-204-null endpoints
- Build green
  </success_criteria>

<output>
Create `.planning/phases/02-order-cart-address/02-01-SUMMARY.md` summarising: nanoid version installed, exact line where 204 branch was inserted, list of routes now benefitting from the bypass (DELETE /cart, DELETE /cart/items/:id, future PATCH /cart/items/:id qty=0).
</output>
