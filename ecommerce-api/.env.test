# Test .env - DO NOT USE IN PRODUCTION
# This file is for testing validation

# App
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# Database
# DB_HOST=localhost  # REMOVED TO TEST VALIDATION
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=root123
DB_DATABASE=ecommerce_db

# JWT
JWT_SECRET=short  # INVALID - Less than 32 characters
JWT_EXPIRATION=7d
JWT_REFRESH_EXPIRATION=30d

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Upload
MAX_FILE_SIZE=5242880
UPLOAD_DEST=./uploads

# Pagination
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=100
